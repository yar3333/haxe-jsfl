package jsfl;

typedef Math =
{
	public function concatMatrix(mat1:Matrix, mat2:Matrix) : Matrix;
	public function invertMatrix(mat:Matrix) : Matrix;
	public function pointDistance(pt1:JSFLPoint, pt2:JSFLPoint) : Float;
}